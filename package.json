{
  "name": "eventemitter-wrapper",
  "version": "1.0.0",
  "description": "Control event groups",
  "type": "commonjs",
  "main": "./index.js",
  "module": "./module.mjs",
  "exports": {
    ".": {
      "import": "./module.mjs",
      "require": "./index.js"
    }
  },
  "scripts": {
    "test": "mocha -t 10000 --recursive tests/**/*.js --check-leaks --use_strict --exit",
    "test-parallel": "mocha -t 10000 --recursive tests/**/*.js --check-leaks --use_strict --parallel --exit",
    "test-watch": "mocha -t 10000 --recursive tests/**/*.js --check-leaks --use_strict --watch --bail --full-trace --trace-deprecation --exit"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Jashepp/eventemitter-wrapper"
  },
  "author": "unchosen.aus@gmail.com",
  "license": "MIT",
  "keywords": [
    "event",
    "eventemitter"
  ],
  "devDependencies": {
    "chai": "^4.3.8",
    "mocha": "^10.2.0",
    "sinon": "^15.2.0",
    "sinon-chai": "^3.7.0",
    "underscore": "^1.13.6"
  }
}
